# Playwright Framework - SDET Portfolio

Framework de testing E2E construido con Playwright + TypeScript, diseñado con foco en arquitectura, mantenibilidad y buenas prácticas de SDET.

## Stack
- Playwright
- TypeScript
- Page Object Model
- Fixtures custom

## Estructura
(detallar la estructura de carpetas)

## Setup
1. Clonar el repo
2. `npm install` 
3. `npx playwright install` 
4. Copiar `.env.example` a `.env` y ajustar valores
5. `npx playwright test` 

## Decisiones de diseño
(documentar las decisiones a medida que se avanza)
